{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "electron": "electron",
    "electron-build": "ng build --prod && npm run electron"
  },
  "author": "leon",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.1.0"
  }
}
